node_modules
.DS_Store
dist
.next
*.local
.vite-inspect
.remote-assets
components.d.ts

# You can save our transcript locally as `.transcript` to avoid uploading to repo
# This way, you can use Cursor or local models to reference the raw transcript when writing notes.
*.transcript

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Event assets - ignore temporary/working files but keep final assets
events/**/temp/
events/**/*.tmp
events/**/*~
events/**/.DS_Store

# Large media files - consider using Git LFS for these
# events/**/*.mov
# events/**/*.mp4
# events/**/*.avi


